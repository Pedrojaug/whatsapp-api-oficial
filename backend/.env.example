# ─── Banco de Dados ────────────────────────────────────────────────────────────
# URL de conexão PostgreSQL (ex: Neon, Supabase, Railway)
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require"

# ─── Servidor ──────────────────────────────────────────────────────────────────
PORT=3001
FRONTEND_URL="http://localhost:5173"
BACKEND_URL="http://localhost:3001"

# ─── Segurança (OBRIGATÓRIO — servidor não inicia sem estas) ───────────────────
# Gere com: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET="sua-chave-jwt-secreta-minimo-32-caracteres"

# Gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY="sua-chave-aes-256-de-32-caracteres"

# ─── Integração Meta / WhatsApp ────────────────────────────────────────────────
FACEBOOK_APP_ID=SEU_APP_ID_AQUI
FACEBOOK_APP_SECRET=SEU_APP_SECRET_AQUI

# Token para validação de webhooks da Meta (você define este valor)
WEBHOOK_VERIFY_TOKEN="um-token-secreto-para-verificar-webhooks"

# ─── Integração n8n (opcional) ─────────────────────────────────────────────────
N8N_SDR_WEBHOOK_URL="https://seu-n8n.dominio.com/webhook/SEU_WEBHOOK_ID"

# Chave de API para autenticar requisições vindas do n8n (você define)
N8N_API_KEY="uma-chave-api-secreta-para-o-n8n"

# ─── Cloudflare R2 / Object Storage (Opcional - Altamente recomendado para economia de banco) ───
# Elimina o armazenamento de Base64 no Postgres da Neon. 10GB grátis/mês + zero custo de download.
# Crie seu bucket R2 no painel Cloudflare > R2 > Create bucket
R2_ACCOUNT_ID="seu_cloudflare_account_id"
R2_ACCESS_KEY_ID="sua_chave_de_acesso_r2"
R2_SECRET_ACCESS_KEY="seu_segredo_de_acesso_r2"
R2_BUCKET_NAME="send-inteligentte-media"
# URL pública do bucket (R2.dev domain ou seu subdomínio customizado ex: https://media.seudominio.com)
R2_PUBLIC_URL="https://pub-xxxxxxxxxxxxxx.r2.dev"
